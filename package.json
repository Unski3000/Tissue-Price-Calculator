{
  "name": "tissue-price-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:js": "node --check calculator.js",
    "check:deploy": "node scripts/check-deploy.mjs"
  }
}
